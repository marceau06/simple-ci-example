name: Simple CI

on:
  push:
    branches:
       - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Checkout repository
        env:
          MESSAGE: ${{ secrets.MESSAGE }}
        run: echo "Hello $MESSAGE"
             echo "Hello ${{ secrets.MESSAGE }}"
